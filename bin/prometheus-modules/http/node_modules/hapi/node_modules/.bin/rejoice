#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../rejoice/bin/rejoice" "$@"
  ret=$?
else 
  node  "$basedir/../rejoice/bin/rejoice" "$@"
  ret=$?
fi
exit $ret
