/*

 var Promise = require("bluebird");
var Core = require("prometheus-core");

module.exports.getServicesForModule = function(module_id){
	//$todo: there should be some kind of access checks so this functions returns an array of only those services that the module ia granted access to
	return Core.getServices();
}

module.exports.emit_service_event = function(service_name, service_event, event_payload){
	return new Promise(function(resolve, reject){
		return Core.getServices()[service_name].emit(service_event, event_payload);
	});
}
*/